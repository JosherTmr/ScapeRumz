[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "escape-room"
version = "0.0.1"
dependencies = [
    "Flask",
    "requests",
    "google-generativeai",
    "gunicorn",
]

[tool.setuptools]
py-modules = ["app"]